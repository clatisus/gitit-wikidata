# Contest Info

date: 2018.05.14 10:16-15:16

# Solutions

# Replay and Summary

## Replay

终榜是不可能出的，只能凭记忆假装 replay 一下了。

M 题直接就秒掉了，还蛮有趣的。W 前期一直觉得 C 可做，花了不少时间手画了前几个 case。Z 跟榜把 K 写了，一开始式子推错了，后来又发现模数写错了，改掉之后有一个加法忘记取模，改掉之后过了。

D 和 W 讨论了一下 A ，感觉马拉车一下似乎就做完了，D 上去写。W 跟榜继续看题，然后把 I 题看错了，以为先最大化价值，再最小化边，就不是树，不会做。然后看了一下 L，觉得可以打表。

D 写完 A 题，因为没处理 n = 1 的情况，wa 了一发然后过了。Z 写了一下 F，打印下来看了看改了改也过了。W 上去写 L，然后 D 和 Z 开始讨论 B。W 把 L 过了之后，Z 觉得 B 可以二维 fft 优化，就开始写。W 和 D 讨论了一下 I ，D 也读错题了，感觉不会，此时榜上 I 和 J 都有人通过。

Z 的 B 题板子抄错了两个地方，W 帮他看的时候只看出了一个，改对之后又 re 和 tle 了几发，挣扎了一会儿也过掉了。此时 W 画了一下 I 的样例，终于发现读错题了！ 和 D 讨论了一下，觉得 dp 一下度数，然后贪心构造就行了。

写完 I 之后，因为 n = 1 的情况错误处理 wa 了几发，Z 来帮忙看的时候看出来了，改掉过了。W 继续乱搞了一会儿 C，不知道为什么假算法可以过 n = 5 的情况，导致没发现要容斥去重。最后 45 min，D 发现 J 也读错题了，可以循环线段树加 dp 搞一搞，写了一下，W 帮忙 de 了一会儿 bug，然后一发过掉了。

总的来说，8 题海星。