## Replay

比赛开始的前 `16min`，发现题面很长，找不到签到题。随后发现有队过了 `J`，于是 `W` 开始签到。并且 `W` 给 `Z` 喂了 `K` 的题意，`Z` 过了一会基本想出来了。中间 `D` 在想 `A` 和 `D`，分别考虑在 `trie` 上 `dp` 和闵可夫斯基和，之后就没有再想了。然后 `J` 愉快地 `wa` 了，打印出来后 `Z` 去写 `K`，很快写完后又 `wa` 了。两人都下来自己看代码，中间很久没看出来，最后甚至怀疑 `output` 中有两个空格，但是不管怎么样都 `wa`。大概 `80min` 的时候 `D` 把 `J` 重写了一遍，过了。怀疑开始时 `fgets` 或者 `substr` 或者 `std::string` 用出问题了。此时 `W` 开始遍历题意，把几乎所有题读了一遍。然后过了几 `min` `Z` 发现 `K` 没考虑 $k>n$，改了之后就过了。然后 `D` 看到没题可写，开始用 `kd` 树莽 `G` 题。过了一会，`Z` 被喂了 `C`，然后推了个式子，半个多小时后过了。中间 `D` 写了一会 `G`，与此同时，`W` 给 `Z` 喂了 `L` 和 `I`，均想出了做法，感觉 `L` 比较难写就往后拖了一阵，大概半个小时把 `I` 过了。中间 `I` 题甚至把一个 $\otimes$ 看成了 $\times$，可以说是很慌了。然后在大概 `180min` 的时候，`G` 写完了，然后立刻 `T` 了。中间 `D` 被 `Z` 换下的时候和 `W` 讨论了一下 `E` 的迷之分块。之后 `Z` 想用 `FFT` 写 `M`，于是 `w` 帮 `Z` 大概抄了 `20min` 的板。然后 `D` 和 `Z` ~~冷静~~地想出了 `G` 的暴力，但是预处理受到一道 `1e18` 的题目的影响，`Z` 写得很奇怪，并且样例过不去。随后`W` 开始上机写分块。之后各种调试之后，`G` `WA` 了。 与此同时 `E` 也有很久本地不过。之后发现了 `G` 的清空有问题，但是改对之后又 `T`了，主要是 `Z` 的预处理太奇怪了。此时已经只剩 `15min` 了，遂弃疗。过了几 `min`，`W` 终于过了样例，但是 `wa` 了。~~Game over~~